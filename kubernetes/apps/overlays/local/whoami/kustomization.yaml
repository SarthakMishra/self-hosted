apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local overlay is self-contained — uses a standard Deployment instead of
# the base's Argo Rollout, avoiding the Argo Rollouts CRD dependency.

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
