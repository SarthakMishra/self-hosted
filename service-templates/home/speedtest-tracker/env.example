# Speedtest Tracker Configuration

# === DOMAIN ===
SPEEDTEST_TRACKER_DOMAIN=speedtest.example.com

# === APPLICATION (required) ===
# Generate with: echo -n 'base64:'; openssl rand -base64 32
APP_KEY=changeme
APP_URL=https://speedtest.example.com
ASSET_URL=https://speedtest.example.com

# === CONTAINER USER ===
PUID=1000
PGID=1000

# === INITIAL ADMIN (only effective during first setup) ===
ADMIN_NAME=Admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme

# === OPTIONAL ===
# APP_NAME=Speedtest Tracker
# APP_TIMEZONE=Europe/London
# DISPLAY_TIMEZONE=Europe/London
# SPEEDTEST_SCHEDULE=6 */2 * * *
# PRUNE_RESULTS_OLDER_THAN=0
