# SerpBear Configuration

# === DOMAIN ===
SERPBEAR_DOMAIN=serpbear.example.com

# === AUTHENTICATION ===
SERPBEAR_USER=admin
SERPBEAR_PASSWORD=changeme

# Generate with: openssl rand -hex 32
SERPBEAR_SECRET=changeme

# Generate with: openssl rand -hex 20
SERPBEAR_APIKEY=changeme

# === APPLICATION ===
NODE_ENV=production
